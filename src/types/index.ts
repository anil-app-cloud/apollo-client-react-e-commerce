export interface Product {
    id: string;
    name: string;
    price: number;
    discount: number;
  }
  
  export interface User {
    id: string;
    name: string;
    email: string;
    address: string;
  }